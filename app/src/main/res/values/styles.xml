<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
         </style>
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/colorPrimaryMaterial</item>

        <!-- Support library compatibility -->
        <item name="background">@color/colorPrimaryMaterial</item>
        <item name="homeAsUpIndicator">@drawable/ic_menu_white_24dp</item>
    </style>

    <style name="AcesMaterial" parent="@style/Theme.AppCompat.NoActionBar" >
        <item name="colorPrimaryDark">@color/primary_dark_material_dark_1</item>
    </style>
</resources>
